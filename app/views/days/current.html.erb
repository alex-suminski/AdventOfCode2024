<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Advent Of Code 2024" %></title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        background-color: #11999b;
        color: white;
        font-size: 40px;
        text-align: center;
        flex-direction: column; 
      }
      .answer-container {
        display: flex; 
        align-items: center; 
        margin-bottom: 20px; 
      }
      .button {
        background-color: #6a0dad; 
        color: white;
        border: none;
        border-radius: 5px;
        padding: 8px 15px; 
        font-size: 25px;
        cursor: pointer; 
        margin-left: 20px; /
        transition: background-color 0.3s;
      }
      .button:hover {
        background-color: #5b0c9e; 
      }
      .response {
        font-size: 30px; 
        margin-top: 30px; 
      }
    </style>
  </head>
  <body>
    <div class="answer-container">
      <span><%= @answer %></span>
      <%= button_to 'Post', { controller: "days", action: "post1", answer: @answer }, { method: :post, class: 'button' } %>
    </div>
    <div class="answer-container">
      <span><%= @answer2 %></span>
      <%= button_to 'Post', { controller: "days", action: "post2", answer: @answer2 }, { method: :post, class: 'button' } %>
    </div>
    <div class="response">
      <%= @congrats_msg || "Fingers Crossed!" %>
    </div>
  </body>
</html>

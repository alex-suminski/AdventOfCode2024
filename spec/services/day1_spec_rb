require 'rails_helper.rb'

RSpec.describe Day1 do    
  let(:task_input_fetcher_double) {instance_double('TaskInputFetcher')}
  let(:subject) {described_class.new(task_input_fetcher: task_input_fetcher_double)}  
       
    it 'works' do   
      allow(task_input_fetcher_double).to receive(:call).with(1).and_return(
        [

        ]
      )
      expect(subject.call).to eq 0
    end
end